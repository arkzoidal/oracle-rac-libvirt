CREATE DISKGROUP ACFS EXTERNAL REDUNDANCY
  DISK
{% for item in hostvars[inventory_hostname].devtags %}
'{{ item.dev }}' NAME {{ item.tag }}{{ "," if not loop.last else "" }}
{% endfor %}
ATTRIBUTE
{% for item in hostvars[inventory_hostname].attrkv %}
'{{ item.name }}' = '{{ item.value }}'{{ "," if not loop.last else "" }}
{% endfor %}
;
