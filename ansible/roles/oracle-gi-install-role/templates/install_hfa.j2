{{ gv_haf_tmp_dir }}/ahf_setup -silent -data_dir {{ gv_haf_data_dir }} -ahf_loc {{ gv_haf_loc_dir }} -tmp_loc {{ gv_haf_tmp_dir }} -nodes {% for host in groups[hostgroup] -%} {{ hostvars[host].node_host_name }}{{ "," if not loop.last else "" }} {%- endfor +%}
